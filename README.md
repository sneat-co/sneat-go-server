# sneat-server

This repo releases Sneat server binaries for local development

## Why we need sneat-go-backend?

The sneat-go-backend contains HTTP handlers, business logic and abstract data access layer.
It does not depend on Firebase and any other vendor-specific packages.
